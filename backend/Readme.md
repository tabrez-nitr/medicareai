add basic utils functions like apiError , apiResponse , asyncHandler 
add cloudinary.js for upload profile photos on cloudinary
add generateStringOtp.js to generate random string during email verification
add sendmail.js to sending important mail like registration mail , verifiaction otp to user
connect mongoodb to project
use multer middleware for profile upload